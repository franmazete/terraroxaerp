import { SharedShell } from "@/components/layout/SharedShell";

export default function PendenciasLayout({ children }: { children: React.ReactNode }) {
  return <SharedShell>{children}</SharedShell>;
}
